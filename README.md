# Money Splitter

A simple expense-splitting app for tracking shared expenses and mahjong sessions.

## Tech Stack
- React + Vite
- Firebase Authentication
- Cloud Firestore

## Run Locally

Prerequisites:
- Node.js
- A Firebase project with Firestore enabled

Install dependencies:

```bash
npm install